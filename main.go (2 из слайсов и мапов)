//Zadacha 2
// Что выведет код?

package main

import "fmt"

var foo []int
var bar []int

func main() {
foo = append(foo, 1)
foo = append(foo, 2)
foo = append(foo, 3)
bar = append(foo, 4)
foo = append(foo, 5)

fmt.Println(foo, bar)
}

// Чтобы понять что здесь происходит мы капнем глубже
