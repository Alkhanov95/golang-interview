//Zadacha 2
// Что выведет код? [1 2 3 5] [1 2 3 4]

package main

import "fmt"

var foo []int
var bar []int

func main() {
foo = append(foo, 1) // [1]
foo = append(foo, 2) // [1 2]
foo = append(foo, 3) // [1 2 3]
bar = append(foo, 4) // bar = [1 2 3 4]
foo = append(foo, 5) // foo = [1 2 3 5]

fmt.Println(foo, bar)
}

/*

Чтобы понять что здесь происходит мы капнем глубже
В Go slice - это просто 'окно' в массив. Если ты изменяешь массив - это может 
повлиять на все срезы, которые на него ссылаются 

Пошаговой разбор :
1 foo = append(foo, 1)
 foo становится [1]
 Gо создает под капотом массив и кладет туда 1
2 foo = append(foo, 2)
 foo - [1 2]
3 foo = append(foo, 3)
 foo - [1 2 3]
 В какой-то момент go расширяет внутрениий массив и копирует туда старые значения. 
 Теперь foo указывает на массив, скажем: [1 2 3 _ _ _] (запас по емкости - capacity)
4 bar = append(foo, 4)
 мы берем foo ([1 2 3]) и делаем append(foo, 4)
 bar теперь [1 2 3 4]
 Важно: bar and foo пока указывает на один и тот же массив в памяти (у них общий 
 базовый массив)
5 foo = append(foo, 5)
 добавляем 5 в foo теперь foo [1 2 3 5]
 это все еще тот же массив что и у bar 
 но bar уже отрезан длиной 4 он помнит свое состояние [1 2 3 4]

*/

------------------------------------------------------------------------------------------------
//Zadacha 2.2
//Что выведет следующий код?
//Объясни, почему так получилось.

package main

import "fmt"

func main() {
    a := []int{10, 20, 30}
    b := a[:2]        // b = [10 20]
    c := append(b, 99) // c = ? 10 20 30 90

    a[1] = 88          // Меняем a[1] //[77 88 30 90]
    c[0] = 77          // Меняем c[0]

    fmt.Println("a:", a)
    fmt.Println("b:", b)
    fmt.Println("c:", c)
}

//вывод : 
//a : 77 88 99
//b : 77 88
//c : 77 88 99


//почему так происходить ?
//👉 Потому что slice и так содержит указатель на массив. 
//Это встроено. append использует этот указатель, и, если хватает места (capacity), 
//просто дописывает данные в тот же массив.

//Почему a[1] = 88 и c[0] = 77 повлияли на другие срезы?
//👉 Потому что все срезы a, b, c смотрят на один и тот же массив. 
//Когда ты меняешь элемент по индексу, ты меняешь сам массив. И все срезы это видят.

//Чтобы понять как это работает и что происходит под капотом  +- нам нужно понять как работает cap
/*
Что такое cap? - сколько можно вместить в массив без создания нового массива (емкость)

//Zadacha 1.3
//Capacity check 

------------------------------------------------------------------------------------------------
package main 

import "fmt"

func main() {
s := []int {1, 2, 3}
t := append(s, 4)
s[0] = 99


fmt.Println("s:", s) //s = 99 2 3
fmt.Println("t:", t) //99 2 3 4
}








